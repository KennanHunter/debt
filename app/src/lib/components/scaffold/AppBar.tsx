import { Center, Group, Header } from "@mantine/core";

const AppBar = () => {
	return (
		<Header height={60} p="xs">
			<Group>
				<Center>Debt</Center>
			</Group>
		</Header>
	);
};

export default AppBar;
